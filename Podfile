# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '10.0'
target 'Cardun' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Cardun

  target 'CardunTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CardunUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'GIFImageView'
  pod 'IKEventSource'
  pod 'Cosmos', '~> 19.0'
end
